; Enforce the `engines` field in package.json — devs running pnpm 9 (or
; an untested 11+ pre-release) get a hard install failure instead of a
; warning, matching the pin already in workflows and Dockerfiles.
engine-strict=true
